import * as React from 'react';
import * as THREE from 'three';
import { PresetsType } from '../helpers/environment-assets';
declare type Props = JSX.IntrinsicElements['group'] & {
    contactShadow?: boolean;
    shadows?: boolean;
    adjustCamera?: boolean;
    environment?: PresetsType;
    intensity?: number;
    controls?: React.MutableRefObject<{
        update(): void;
        target: THREE.Vector3;
    }>;
};
export declare function Stage({ children, controls, contactShadow, shadows, adjustCamera, environment, intensity, ...props }: Props): JSX.Element;
export {};
